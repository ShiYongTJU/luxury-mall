version: '3.8'

# 生产环境配置 - 使用 PostgreSQL 数据库
# 使用方法: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    environment:
      - USE_DATABASE=true
      - NODE_ENV=production


