# JWT 配置
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS 配置（多个域名用逗号分隔）
CORS_ORIGIN=http://localhost:80,http://your-domain.com

# 后端端口
PORT=3001

# 环境
NODE_ENV=production

# 数据库配置
# 开发环境：设置为 false 使用 JSON 文件存储（简单快速）
# 生产环境：设置为 true 使用 PostgreSQL 数据库（安全可靠）
USE_DATABASE=false
DB_HOST=postgres
DB_PORT=5432
DB_NAME=luxury_mall
DB_USER=postgres
DB_PASSWORD=your-database-password-change-this

